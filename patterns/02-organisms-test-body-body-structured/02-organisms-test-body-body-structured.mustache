{{! if there is a 'body' key in the data - the organism-body partial will pick it up and render it
the value of the 'body' can be a string or a structured array of conten}}
{{! This particular partial renders the 'body' element based on the structured data provided}}
{{! see organisms/test-body/body-markup.mustache for a simple markup version
that uses the same partial}}
{{&gt; organisms-body}}
